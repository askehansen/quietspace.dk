{
  "name": "quietspace",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.1.1",
    "esbuild": "^0.15.14"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
